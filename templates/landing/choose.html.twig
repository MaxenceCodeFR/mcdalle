{% extends "base.html.twig" %}

{# {% block title %}{{products.categories.name}}{% endblock %} #}

{% block body %}
<div class="d-flex flex-direction-row gap-5 mainCourse">
    {% for product in products %}
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h2 class="card-title">{{product.name}}</h2>
                <p class="card-text">{{product.price}} $</p>
                <div class="d-flex">
                    <button class="btn btn-danger card-link w-3" >Ajouter au panier</button>
                    <a href="{{path('details', {id:product.id})}}" class="card-link">Détail du produit</a>
                </div>
            </div> 
        </div>
    {% endfor %}
</div>
{% endblock %}