{% extends "base.html.twig" %}
{% block title %}Nos Boissons{% endblock %}
{% block body %}
<h1 class="registrationTitle">Quelle type d'ivrogne êtes-vous ?</h1>
{% include "commons/_basket.html.twig" %}
<div class="burgerCategory text-light">
    <h2 class="burgerTitle">{{categories.name}}</h2>

    <div class="subBurgerCategory">
    {% for subcategory in subcategories %}
        <ul>
            <li><a href="{{path('drinks_subcategory', {id:subcategory.id})}}" class="links">{{subcategory.name}}</a></li>
        </ul>
    {% endfor %}
    </div>
    <a href="{{path('landing')}}" class="btn btn-warning w-25 m-2">Choisir sa burger</a>
    <a href="{{path('desserts')}}" class="btn btn-info w-25 m-2">Choisir son dessert</a>
</div>
{% endblock %}